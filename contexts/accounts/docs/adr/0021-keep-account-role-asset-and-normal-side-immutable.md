# Keep Account Role, Asset, and Normal Side Immutable

An Account's Role, Asset, and Normal Side are fixed at creation because changing any of them would reinterpret historical activity and balances. A changed economic purpose uses a Successor Account and explicit transfer Transactions rather than mutating the existing Account.

# Scope Device Identity by Namespace

A Payment Device is uniquely identified within an Organization by its Device Scheme, Identifier Namespace, and normalized value. This supports globally structured identifiers, provider-local tokens, and Organization-local tags without assuming unrelated schemes, providers, or Organizations share one value namespace.

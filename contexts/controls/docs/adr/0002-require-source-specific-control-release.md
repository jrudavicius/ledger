# Require Source-Specific Control Release

Every adjustment or end action targets exactly one Restriction and requires authority appropriate to that Restriction's source; the service provides no generic unblock operation. This prevents an actor authorized for a Customer-Requested Arrangement Freeze from accidentally or deliberately removing an independent legally motivated Restriction, while retaining the actor, authority, time, and reason for each change.
